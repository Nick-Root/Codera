// import { useSelector, useDispatch } from "react-redux";
// import { getQuestionComments } from "../../redux/comment"
// import { useEffect } from "react";

const Comments = ({ questionId }) => {
    // const dispatch = useDispatch()
    // const question = useSelector((state) => state.question.oneQuestion || [])
    // console.log("%c   LOOK HERE", "color: blue; font-size: 18px", question);

    // useEffect(() => {
    //     dispatch(getQuestionComments(questionId));
    // }, [dispatch, questionId]);




    return (
        <>TEST</>
    )
}

export default Comments
